// Fixture for run-static-guards.mjs (card 61d75a3c) — NOT a real guard, never added to
// STATIC_GUARD_REPO_PATHS. A genuinely-passing guard: only PASS lines, exit 0. The negative control for
// the false-green detector — this must NOT be flagged. Invoked only via run-static-guards.mjs's --paths
// test seam (see run-static-guards-false-green.mjs).
console.log("PASS  fixture check one");
console.log("PASS  fixture check two");
process.exit(0);
