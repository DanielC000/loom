# c36bac53 — `parked-background`: a self-reported flag with no backing row, checked after the wake lookup

## Narrative

`parked-background` — card c36bac53: the worker's report itself SELF-ATTRIBUTED the park via `worker_report({..., awaiting: "background"})` — it kicked off a backgrounded SHELL command (or any other async continuation) and is relying on ITS OWN completion (or the harness's on-completion re-invoke) to resume, not the manager. This is genuinely UNDETECTABLE from any daemon-observable state otherwise: the daemon has no API into the engine's background-task registry (confirmed in orchestration/resume-nudge.ts — a `claude --resume` kills any in-flight `run_in_background` shell precisely because Loom has no visibility into it, only the OS process tree). The wired hook surface (SessionStart/UserPromptSubmit/Stop/StopFailure; PreToolUse matcher-scoped to worker_report/memory_write; SubagentStart/SubagentStop for the drift cross-check, card e6ef5062 — see SubagentDriftTracker's own doc, pty/tool-attribution.ts; PostToolUse for the vault-lint matcher and the always-on decision-records Read matcher, card 661b7d46) STILL captures nothing here: SubagentStart/Stop only bound a SYNCHRONOUS, same-turn Task-tool sub-agent call — Claude Code blocks the invoking turn until that tool call itself returns (see tool-attribution.ts's own ORDERING GUARANTEE), so by the time a worker's OWN turn ends (Stop) any sub-agent it invoked that turn has necessarily already stopped too. None of the wired hooks fire for, or say anything about, a `run_in_background` shell or any other continuation that outlives its own invoking turn. UNLIKE `parked-wake`, this flag is a bare, self-reported claim with NO backing row and NO expiry of its own — checked AFTER the wake lookup now (round-2 CR fix): a pending wake is Loom's OWN verifiable, bounded resume signal (it drives the actual resume and names a concrete `wakeAt`), so it wins over the worker's unbacked self-attribution whenever both are present, not the other way around.

## Do not

- Do not treat a worker's self-reported `awaiting:"background"` flag (card c36bac53) as more trustworthy than a pending `wake_me` — check the wake lookup FIRST; a pending wake is Loom's own verifiable, bounded resume signal and wins over the worker's unbacked self-attribution whenever both are present.
- Do not expect any wired hook (SessionStart/UserPromptSubmit/Stop/StopFailure/PreToolUse/SubagentStart/SubagentStop/PostToolUse) to capture a `run_in_background` shell or any other continuation that outlives its own invoking turn — none of them do; the flag is genuinely undetectable from daemon-observable state otherwise.

## Source

Inline comment in `packages/daemon/src/sessions/service.ts` (`classifyIdleWorker`): lines 12772-12792, as of commit `a07c5092c871d47f25aeb2ec160958306294a043`. Relocated by card `3f40210c`; no wording changed, wrapped source lines joined into a flowing paragraph (including one mid-token wrap, `worker_report/` continuing as `memory_write` — joined with no inserted space), the leading list-bullet marker and `*` comment markers stripped. Cards `e6ef5062` and `661b7d46` are referenced here as related context (the hook wiring this decision reasons about) rather than covered by this record — out of this record's scope.
